/**
 * next.config.js
 *
 * Basic Next.js configuration for an App Router project.
 * Add any additional configuration (image domains, rewrites, env) here.
 */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  // If you plan to use images from external domains, add them here:
  // images: { domains: ['images.unsplash.com', 'your-cdn.com'] },
};

module.exports = nextConfig;
